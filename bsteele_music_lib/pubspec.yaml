name: bsteeleMusicLib
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0-179.0.dev <3.0.0'

dependencies:
  logger: ^0.9.4
  quiver: ^2.1.5
  http: ^0.12.2  # not nullable
  archive: ^2.0.13  # not nullable
  universal_html: ^1.2.3  # not nullable
  string_similarity: ^1.1.0  # not nullable
  xml: 4.5.1
  system_info: ^0.1.3  # not nullable
  intl: 0.16.1

dev_dependencies:
  pedantic: ^1.9.2
  test: ^1.15.7
  build_web_compilers: ^2.16.0  # not nullable
  build_runner: ^1.10.11  # not nullable
